# Netscan
Scans any IP or hostname for open TCP ports Detects services (SSH, HTTP, MySQL, RDP, Redis, etc.) Grabs banners from open ports Rates each port LOW / MEDIUM / HIGH risk Quick presets + custom port ranges Clean terminal-style UI that actually looks professional
